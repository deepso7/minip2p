{
  "name": "@minip2p/test-fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../vitest.config.ts --root .",
    "test:watch": "vitest --config ../vitest.config.ts --root .",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
